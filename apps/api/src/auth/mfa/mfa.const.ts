/** Number of backup codes generated when MFA is enabled. */
export const BACKUP_CODES_COUNT = 8;

/** Issuer name displayed in authenticator apps. */
export const MFA_ISSUER = 'iHelp';

/** Length of each backup code in bytes (hex-encoded = 2x characters). */
export const BACKUP_CODE_BYTES = 4;
